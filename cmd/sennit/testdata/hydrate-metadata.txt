✓ Hydrated 1,008 records to depth metadata (25.1s)

Indexer                https://sia.storage
Records                1,008 from 14 objects, 1.92 MiB of ciphertext
Held on this device    1,008
Conversations rebuilt  4
Searchable by meaning  0
Slabs tracked          14

 WARNING  4 conversations were rebuilt from their transcripts
  The messages are exact. A conversation's own description is
  not on Sia, so these were reconstructed here: version, title,
  created, updated, kind. These are not recoverable: summary,
  tags, project, archived, agent, agentRef, models,
  counts.tokens, counts.durationMs, lineage, preservedTail.

 HINT   everything is here except the search vectors; run again
        with `--depth index` to search by meaning
