# Build artifacts
*.exe
*.dll
*.so
*.dylib
*.test
*.out

# Dependencies
node_modules/
vendor/

# Environment / secrets, never commit
.env
.env.*
*.key
*.pem
.phrase
.appid
.appkey

# Local state
*.db
*.sqlite
*.sqlite3
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# Demo captures are point-in-time: they hard-code timings, protocol revisions and
# record ids that go stale on the next change. Record one when you need it.
demo/recordings/

# Built binaries (were committed once, 81.5 MB, never again)
/sennit
/sennit-mcp
