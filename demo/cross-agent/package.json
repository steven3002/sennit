{
  "name": "sennit-cross-agent-check",
  "private": true,
  "type": "module",
  "description": "Reads a Sennit vault with the TypeScript MCP SDK, to show the memory is not bound to the tool that wrote it.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
